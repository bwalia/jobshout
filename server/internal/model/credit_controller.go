package model

// Credit Controller Agent — AP invoice mailbox, AI batch generation, and
// durable exception workflow (proxied to the AIVC reference agents runtime).
const (
	BuiltinCreditController = "credit_controller"
	AgentNameCreditController = "Credit Controller Agent"
)
